git add README.md
git add Test.md 
git branch
git checkout -b draft
git checkout draft
git checkout master
git checkout master
git commit -a -m "Weer extra tekst toegevoegd"
git commit -m "Merged master and draft"
git config
git config --list
git config --local user.email "bidnightsnaq@outlook.com"
git config --local user.name "orfalchechor"
git init
git merge draft
git push
git push -u origin master
git remote add origin https://github.com/orfalchechor/datascientist1.git
git show
git status
git version
git remote -v
